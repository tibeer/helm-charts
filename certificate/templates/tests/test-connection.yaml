apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "certificate.fullname" . }}-test-connection"
  labels:
    {{- include "certificate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "certificate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
