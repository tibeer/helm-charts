apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "snikket.fullname" . }}-test-connection"
  labels:
    {{- include "snikket.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "snikket.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
