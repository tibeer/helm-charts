apiVersion: v1
kind: Service
metadata:
  name: {{ include "theia-blueprint.fullname" . }}
  labels:
    {{- include "theia-blueprint.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "theia-blueprint.selectorLabels" . | nindent 4 }}
