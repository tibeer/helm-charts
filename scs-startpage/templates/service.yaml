apiVersion: v1
kind: Service
metadata:
  name: {{ include "scs-startpage.fullname" . }}
  labels:
    {{- include "scs-startpage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scs-startpage.selectorLabels" . | nindent 4 }}
