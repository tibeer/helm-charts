apiVersion: v1
kind: Service
metadata:
  name: {{ include "openvpn-as.fullname" . }}-http
  labels:
    {{- include "openvpn-as.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.http.type }}
  ports:
    - port: {{ .Values.service.http.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openvpn-as.selectorLabels" . | nindent 4 }}
