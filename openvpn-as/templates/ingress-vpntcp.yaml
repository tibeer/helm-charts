{{- if .Values.ingress.vpntcp.enabled -}}
{{- $fullName := include "openvpn-as.fullname" . -}}
{{- $svcPort := .Values.service.vpntcp.port -}}
{{- if and .Values.ingress.vpntcp.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.ingress.vpntcp.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.ingress.vpntcp.annotations "kubernetes.io/ingress.class" .Values.ingress.vpntcp.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-vpntcp
  labels:
    {{- include "openvpn-as.labels" . | nindent 4 }}
  {{- with .Values.ingress.vpntcp.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.ingress.vpntcp.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.ingress.vpntcp.className }}
  {{- end }}
  {{- if .Values.ingress.vpntcp.tls }}
  tls:
    {{- range .Values.ingress.vpntcp.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.vpntcp.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-vpntcp
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-vpntcp
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
