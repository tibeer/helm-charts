{{- if .Values.ingress.vpnudp.enabled -}}
{{- $fullName := include "openvpn-as.fullname" . -}}
{{- $svcPort := .Values.service.vpnudp.port -}}
{{- if and .Values.ingress.vpnudp.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.ingress.vpnudp.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.ingress.vpnudp.annotations "kubernetes.io/ingress.class" .Values.ingress.vpnudp.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-vpnudp
  labels:
    {{- include "openvpn-as.labels" . | nindent 4 }}
  {{- with .Values.ingress.vpnudp.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.ingress.vpnudp.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.ingress.vpnudp.className }}
  {{- end }}
  {{- if .Values.ingress.vpnudp.tls }}
  tls:
    {{- range .Values.ingress.vpnudp.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.vpnudp.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-vpnudp
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-vpnudp
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
